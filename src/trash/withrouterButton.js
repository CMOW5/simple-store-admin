/*
    <this.MyButton >dsadsa</this.MyButton>

    MyButton = withRouter(({ history }) => (
        <button
          type='button'
          onClick={() => { history.push('/catalog') }}
        >
          Click Me!
        </button>
    ))
    */
